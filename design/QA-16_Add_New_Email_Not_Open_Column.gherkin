Feature: Add new columns to email tracking data

  Scenario: Add NOT_OPENED_EMAIL column
    Given an email tracking table with columns IS_CLICKED and IS_OPENED
    When a row has IS_CLICKED = TRUE and IS_OPENED = FALSE
    Then set the NOT_OPENED_EMAIL column to TRUE for that row

  Scenario: Add _timestamps_ column
    Given an email tracking table
    When adding a new column
    Then add a column named _timestamps_ with data type in seconds
