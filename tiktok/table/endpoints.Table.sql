
CREATE TABLE endpoints (
	appname VARCHAR(256) NOT NULL, 
	display_name VARCHAR(256), 
	endpoints_url VARCHAR(1000), 
	input VARCHAR(1000), 
	output VARCHAR(1000), 
	end_point VARCHAR(500), 
	display_option VARCHAR(200)
)

