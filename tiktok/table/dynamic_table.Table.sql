
CREATE TABLE dynamic_table (
	app_name VARCHAR(256) NOT NULL, 
	api_key VARCHAR(256), 
	channel_id VARCHAR(256), 
	endpoint_seq VARCHAR(256), 
	start_date DATE, 
	history BOOLEAN, 
	end_point VARCHAR(500), 
	competitor VARCHAR(2000), 
	display_option VARCHAR(200), 
	access_toke VARCHAR(200), 
	access_token VARCHAR(200), 
	twitter_handle_name VARCHAR(200), 
	page_id VARCHAR(200)
)

