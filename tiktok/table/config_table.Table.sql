
CREATE TABLE config_table (
	app_name VARCHAR(256), 
	process_name VARCHAR(256), 
	history VARCHAR(256), 
	start_date TIMESTAMPTZ, 
	competitors VARCHAR(256), 
	endpoint_name VARCHAR(258), 
	days_threshold VARCHAR(258)
)

