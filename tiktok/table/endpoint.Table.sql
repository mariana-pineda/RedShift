
CREATE TABLE endpoint (
	app_name VARCHAR(256), 
	process_name VARCHAR(256), 
	sequence INTEGER, 
	endpoint_name VARCHAR(256), 
	endpoint_url VARCHAR(2000), 
	input VARCHAR(2000), 
	output VARCHAR(2000), 
	display_option VARCHAR(256), 
	display_name VARCHAR(258), 
	type VARCHAR(258)
)

